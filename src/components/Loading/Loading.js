function Loading() {
  return (
    <h2
      style={{
        textAlign: "center",
        background: "cyan",
        color: "blue",
        borderRadius: "22px",
        padding: "5px",
        paddingBottom: "10px",
        marginTop: "10px",
      }}
    >
      Loading...
    </h2>
  );
}

export default Loading;
