function Loading() {
  return (
    <h1
      style={{
        textAlign: "center",
        color: "blue",
        borderRadius: "22px",
        marginTop: "20px",
        fontFamily: "Fantasy, Arial, Helvetica, sans-serif",
        fontSize: "44px",
      }}
    >
      LOADING . . .
    </h1>
  );
}

export default Loading;
